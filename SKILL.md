---
name: self-learning-review
description: AI Coding 错题本 / 复盘生成器。将一次 AI Coding（Claude Code / Cursor / ChatGPT 等）开发或 Debug 过程转化为结构化学习复盘：时间线还原、5 Whys 根因链、失败尝试记录、AI 协作复盘、行动项与复习计划。当用户说"帮我复盘"、"总结一下开发过程"、"整理这个 bug"、"生成学习记录"、"记个错题"，或表示"折腾很久终于解决了"想沉淀经验时使用。
---

# AI Coding 错题本 / 复盘生成器

## 定位

把一次 AI Coding 过程转化为结构化学习复盘，让用户从"让 AI 完成任务"变成"理解并掌握解决问题的方法"。

两条铁律：

1. **不总结代码，总结思考**：还原决策过程、问题因果链、可迁移的方法。不逐文件讲改动、不贴大段代码、不罗列 commit
2. **无责复盘（blameless）**：默认当时每个人（包括用户自己和 AI）在已有信息下做了合理选择。不写"我不该这么蠢"、"AI 太坑"；把"为什么当时没想到"改写成"下次出现什么信号就该警觉"

设计依据见 [references/methodology.md](references/methodology.md)（可选阅读，不影响执行）。

## 两种模式

| 模式 | 何时用 | 模板 |
|---|---|---|
| 深度复盘（默认） | 完整功能开发、卡顿较久的 bug、多次绕路 | [templates/review-template.md](templates/review-template.md) |
| 快速复盘 | 小 bug、用户赶时间、说"简单记一下" | [templates/quick-review-template.md](templates/quick-review-template.md) |

拿不准就用深度模式。

## 工作流

开始时把下面的 checklist 复制进回复，随进度打勾：

```
复盘进度：
- [ ] 1 收集信息
- [ ] 2 还原时间线
- [ ] 3 挖根因
- [ ] 4 提炼知识
- [ ] 5 AI 协作复盘
- [ ] 6 生成文档
- [ ] 7 质量自检
- [ ] 8 交付
```

### 1 收集信息

按优先级使用可得的输入（有一项即可开始，多项更好）：

| 输入来源 | 说明 |
|---|---|
| 当前会话 | 这次开发就发生在当前对话中，直接回顾全过程 |
| 对话记录 | 用户粘贴的 Claude Code / Cursor / ChatGPT 对话导出 |
| 用户描述 | 用户口头描述做了什么、卡在哪、怎么解决的 |

如果信息不足以还原"问题 → 尝试 → 解决"链路，向用户追问（一次最多 3 个问题，问完即开始，不反复追问）：

1. 这次任务的目标是什么？
2. 中间遇到了什么报错或卡点？最后是怎么解决的？
3. 有没有尝试过但失败的办法？

**MVP 范围限制**：不做 Git 自动分析、数据库存储、自动提醒。仅当用户主动贴出 diff/log 时可作为辅助参考。

### 2 还原时间线

按时间顺序列出做过的事，标注性质：🔵 正常推进 / 🟡 绕路 / 🔴 卡点。

时间线是后续所有分析的底座——🟡 和 🔴 就是复盘的重点素材。口语化输入往往顺序混乱，先理清"实际发生了什么、按什么顺序"。

### 3 挖根因（5 Whys）

对每个 🔴/🟡 事件，从表象出发连问"为什么"，每层答案必须能被"当时的信息"支持、不允许跳步，直到落在**值得修的那一层**——再往上就变成"人为什么会犯错"这类无法行动的层面。

- 判断标准：挖到"改了它问题就消失，且下次能提前检查"为止
- 因果不唯一时写多条链并行，不硬凑单链
- 根因 ≠ 表面结论："装了包就好了"是表象，"为什么这个环境缺包、为什么缺了没被发现"才是根因方向

### 4 提炼知识

**技术知识**：学习主题 + 理解程度（四级标尺）+ 关键点。

- 理解程度：L1 能看懂 / L2 能复述 / L3 能独立做 / L4 能教别人
- 关键点必须**用自己的话**写，不是复述 AI 的原话——搬来的解释检测不出理解缺口

**Debug 方法**：问题类型 → 排查顺序。从这次实际路径抽象；走错过的分支要体现"为什么不先走那条"。

**开发经验**：场景 → 规则。

### 5 AI 协作复盘

回看用户发给 AI 的 Prompt / 指令，评估协作质量：

- **做得好**：哪些信息给得足（技术栈、功能目标、限制条件……）
- **可以改进**：缺了什么导致来回沟通（验收标准、边界条件、运行环境、报错原文……）
- **推荐 Prompt 模板**：针对本次任务类型，给出下次可直接复用的模板

评价必须基于实际对话内容，不套空话。此节同样遵守无责原则：AI 猜错方向通常源于关键上下文缺失，把重点放在"补什么信息"而不是"怪 AI 笨"。

### 6 生成文档

读取对应模板，逐节填写：

- 头部信息：日期默认当天，主题一句话概括，技术栈从输入推断（不确定则留空请用户补充）
- 标签：3-5 个 `#` 标签（技术域 + 问题类型），便于日后检索
- 每个问题必须填"下次更早发现的信号"：当时做法合理的条件下，下次什么信号能提前预警
- 行动项必须是"触发条件 → 动作"格式，可执行、可检查
- **自我检验节是本 skill 的价值核心**：针对根因和关键概念出 1-2 个问题，留给用户盲答（AI 不要替用户答），并提醒"答完对照第 3、5 节订正"——能自己讲清楚才算学会
- 复习计划固定三档：3 天 / 1 周 / 1 个月（间隔复习）

### 7 质量自检

输出前逐条过，任何一条不通过就修改后重审：

- [ ] H1/H2 结构与所选模板完全一致
- [ ] 每个值得记录的问题都有失败尝试记录（信息缺失时注明"未记录"）
- [ ] 根因链无跳步，且不是"改了就好了"式的表层结论
- [ ] "做得好"至少 1 条（复盘不是自我批评大会）
- [ ] 行动项全部是"触发条件 → 动作"格式
- [ ] 自我检验问题针对根因而非表面现象
- [ ] 无责措辞：没有对用户或 AI 的指责性表述
- [ ] 内容全部来自用户输入与会话记录，不确定的细节标"待确认"，不臆造
- [ ] 关键点用自己的话写，不是 AI 原话搬运
- [ ] 语言用中文，保留必要的英文技术术语；篇幅标准：三个月后的自己还能看懂

### 8 交付

1. 展示完整 Markdown 给用户
2. 询问是否保存为文件；用户同意则：
   - 写入 `reviews/YYYY-MM-DD-<主题>.md`（或用户指定路径）
   - 在 `reviews/INDEX.md` 追加一行索引：`| 日期 | 主题 | 标签 | [文件](链接) |`（纯文件级轻量实现，不是数据库；INDEX.md 不存在则创建并加表头）

## 高价值问题识别

只复盘值得复盘的问题，重点识别：

- 卡了很久才解决的
- 尝试了多个方案才成功的
- 根因和表象不一致的（配置、环境、缓存、版本类）
- AI 第一次给错方向、后来才修正的

## 场景差异

| 场景 | 模式 | 侧重 |
|---|---|---|
| 完整功能开发 | 深度 | 时间线 + 技术知识 + AI 协作 |
| 复杂 bug 修复 | 深度 | 根因链 + 尝试记录 + Debug 方法 |
| 小 bug / 赶时间 | 快速 | 一页卡片：现象 → 根因 → 规则 |
| AI 对话复盘 | 深度 | 重点做第 7 节 AI 协作复盘，其余从简 |

## 主动建议（可选）

协助其他任务时发现以下信号，可主动提示一次（用户拒绝就不再提）：

- 同一问题反复修复多次
- 长时间 Debug 后终于解决

> 这次解决过程包含较多学习价值，是否生成复盘记录？
