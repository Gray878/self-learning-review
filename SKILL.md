---
name: self-learning-review
description: AI Coding 错题本 / 复盘生成器。将一次 AI Coding（Claude Code / Cursor / ChatGPT 等）开发或 Debug 过程转化为结构化学习复盘：时间线还原、5 Whys 根因链、失败尝试记录、行动项与自我检验。当用户说"帮我复盘"、"总结一下开发过程"、"整理这个 bug"、"生成学习记录"、"记个错题"，或表示"折腾很久终于解决了"想沉淀经验时使用。
---

# AI Coding 错题本 / 复盘生成器

## 定位

把一次 AI Coding 过程转化为结构化学习复盘，让用户从"让 AI 完成任务"变成"理解并掌握解决问题的方法"。

两条铁律：

1. **不总结代码，总结思考**：还原决策过程、问题因果链、可迁移的方法。不逐文件讲改动、不贴大段代码、不罗列 commit
2. **无责复盘（blameless）**：默认当时每个人（包括用户自己和 AI）在已有信息下做了合理选择。不写"我不该这么蠢"、"AI 太坑"；把"为什么当时没想到"改写成"下次出现什么信号就该警觉"

设计依据见 [references/methodology.md](references/methodology.md)（可选阅读，不影响执行）。

## 两种模式

| 模式 | 何时用 | 模板 |
|---|---|---|
| 深度复盘（默认） | 完整功能开发、卡顿较久的 bug、多次绕路 | [templates/review-template.md](templates/review-template.md) |
| 快速复盘 | 小 bug、用户赶时间、说"简单记一下" | [templates/quick-review-template.md](templates/quick-review-template.md) |

拿不准就用深度模式。

## 工作流

开始时把下面的 checklist 复制进回复，随进度打勾：

```
复盘进度：
- [ ] 1 收集信息
- [ ] 2 还原时间线 + 确认记录范围
- [ ] 3 挖根因（只对确认的问题）
- [ ] 4 提炼关键学习点
- [ ] 5 生成文档
- [ ] 6 质量自检
- [ ] 7 交付
```

### 1 收集信息

按优先级使用可得的输入（有一项即可开始，多项更好）：

| 输入来源 | 说明 |
|---|---|
| 当前会话 | 这次开发就发生在当前对话中，直接回顾全过程 |
| 对话记录 | 用户粘贴的 Claude Code / Cursor / ChatGPT 对话导出 |
| 用户描述 | 用户口头描述做了什么、卡在哪、怎么解决的 |

如果信息不足以还原"问题 → 尝试 → 解决"链路，向用户追问（一次最多 3 个问题，问完即开始，不反复追问）：

1. 这次任务的目标是什么？
2. 中间遇到了什么报错或卡点？最后是怎么解决的？
3. 有没有尝试过但失败的办法？

**MVP 范围限制**：不做 Git 自动分析、数据库存储、自动提醒。仅当用户主动贴出 diff/log 时可作为辅助参考。

### 2 还原时间线 + 确认记录范围

按时间顺序列出做过的事，标注性质：🔵 正常推进 / 🟡 绕路 / 🔴 卡点。

时间线是后续所有分析的底座——🟡 和 🔴 就是复盘的重点素材。口语化输入往往顺序混乱，先理清"实际发生了什么、按什么顺序"。

**不要把当前会话的所有问题都记进去。** 从 🟡/🔴 里挑出值得记的，摆成候选清单（每条附一句"为什么值得记"），一次性跟用户确认保留哪些，再继续：

- 候选标准（命中任一条即值得提）：卡了很久才解决的 / 尝试了多个方案才成功的 / 根因和表象不一致的（配置、环境、缓存、版本类）/ AI 第一次给错方向、后来才修正的 / 用户明确想沉淀的
- 拿不准时直接问"这几个问题里记录哪几个？"，或给默认推荐让用户勾选
- 确认后**只对保留的问题**做第 3 步挖根因；用户没选的一律不写进文档

### 3 挖根因（5 Whys）

只对第 2 步确认要记录的问题挖根因。从表象出发连问"为什么"，每层答案必须能被"当时的信息"支持、不允许跳步，直到落在**值得修的那一层**——再往上就变成"人为什么会犯错"这类无法行动的层面。

- 判断标准：挖到"改了它问题就消失，且下次能提前检查"为止
- 因果不唯一时写多条链并行，不硬凑单链
- 根因 ≠ 表面结论："装了包就好了"是表象，"为什么这个环境缺包、为什么缺了没被发现"才是根因方向

### 4 提炼关键学习点

2-3 条，每条一句话，只留能直接迁移到下一个任务的规则（技术知识与排查经验混编即可，不必分门别类）。用自己的话写——搬 AI 原话检测不出理解缺口，宁可少写也不要堆细节。

### 5 生成文档

读取对应模板，逐节填写：

- 头部信息：YAML frontmatter 写 `date`（默认当天）和 `tags`（3-5 个：技术域 + 问题类型）；正文首行主题一句话概括，技术栈从输入推断（不确定则留空请用户补充）
- 第 3 节"遇到的问题"只填用户确认要记录的问题，每个问题一节，控制篇幅；确认的问题较多时写最重要的，其余一句带过
- 每个问题必须填"下次更早发现的信号"：当时做法合理的条件下，下次什么信号能提前预警
- 行动项必须是"触发条件 → 动作"格式，可执行、可检查
- **自我检验节是本 skill 的价值核心**：针对根因和关键概念出 1-2 个问题，留给用户盲答（AI 不要替用户答），并提醒"答完对照第 3、4 节订正"——能自己讲清楚才算学会

### 6 质量自检

输出前逐条过，任何一条不通过就修改后重审：

- [ ] H1/H2 结构与所选模板完全一致
- [ ] 问题只包含用户确认要记录的部分（第 3 节与第 2 步确认的范围一致，未确认的不在）
- [ ] 每个确认记录的问题都有失败尝试记录（信息缺失时注明"未记录"）
- [ ] 根因链无跳步，且不是"改了就好了"式的表层结论
- [ ] 行动项全部是"触发条件 → 动作"格式
- [ ] 自我检验问题针对根因而非表面现象
- [ ] 无责措辞：没有对用户或 AI 的指责性表述
- [ ] 内容全部来自用户输入与会话记录，不确定的细节向用户追问，不臆造补写
- [ ] 关键点用自己的话写，不是 AI 原话搬运
- [ ] 语言用中文，保留必要的英文技术术语；篇幅标准：三个月后的自己还能看懂

### 7 交付

1. 展示完整 Markdown 给用户，询问保存在哪，按优先级：
   1. 用户当次指明的路径
   2. 已配置的 Obsidian vault（见下）
   3. 默认：当前项目 `reviews/`
2. Obsidian vault 配置（一次性，持久化）：
   - 配置文件：`~/.claude/skills/self-learning-review/config.md`（与 SKILL.md 同级；安装更新只镜像 SKILL.md / templates / examples / references，不会动 config.md，故配置不丢）
   - 已配置：config.md 存在且有 `vault:` 行 → 直接用该路径
   - 首次配置（用户提到 Obsidian / 笔记库但无 config）：按顺序确定 vault 并写回 config.md：
     1. 读 `%APPDATA%/obsidian/obsidian.json`（macOS：`~/Library/Application Support/obsidian/obsidian.json`）取 `vaults` 字段
     2. 一个 vault → 确认后用；多个 → 列出让用户选；读不到 → 请用户直接贴 vault 文件夹路径
     3. 写入 config.md 一行：`vault: <路径>`
   - 更换：用户说"换 vault"或原路径已不存在 → 重走首次配置，覆盖 config.md
3. 保存规则：
   - 文件名 `YYYY-MM-DD-<主题>.md`
   - **上级目录用当前项目名**（取 cwd 目录名；用户当次指定则覆盖），不用固定文件夹名。存 vault 时即 `<vault>/<项目名>/`——同一项目的复盘聚在一起，Obsidian 图谱按项目成簇
   - 默认（无 vault）：当前项目 `reviews/`
   - 同目录 `INDEX.md` 追加：`| 日期 | [[YYYY-MM-DD-主题]] | tags |`（Obsidian 双向链接；INDEX.md 不存在则创建并加表头）
   - frontmatter 的 `tags` 是 Obsidian 原生标签特性，无需额外处理

## 场景差异

| 场景 | 模式 | 侧重 |
|---|---|---|
| 完整功能开发 | 深度 | 时间线 + 关键学习点 + 行动项 |
| 复杂 bug 修复 | 深度 | 根因链 + 尝试记录 |
| 小 bug / 赶时间 | 快速 | 一页卡片：现象 → 根因 → 规则 |

## 主动建议（可选）

协助其他任务时发现以下信号，可主动提示一次（用户拒绝就不再提）：

- 同一问题反复修复多次
- 长时间 Debug 后终于解决

> 这次解决过程包含较多学习价值，是否生成复盘记录？
